import { Transform } from '@gltf-transform/core';
export interface PartitionOptions {
    meshes: Array<string>;
}
declare const partition: (options: PartitionOptions) => Transform;
export { partition };
